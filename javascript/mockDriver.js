// Generated by CoffeeScript 1.9.2
(function() {
  var dbs, promisify_db,
    hasProp = {}.hasOwnProperty;

  require('../../openbeelab-util/javascript/stringUtils').install();

  promisify_db = require('./promisify_dbDriver');

  exports.dbs = dbs = {
    _users: {}
  };

  exports.connectToServer = function(config) {
    var updateViews;
    updateViews = function(db) {
      var _, candidate, docs, emit, ev, isEmitted, name, outKey, outValue, ref, ref1, res, results, view;
      ref = db.views;
      results = [];
      for (name in ref) {
        if (!hasProp.call(ref, name)) continue;
        view = ref[name];
        emit = function(key, value) {
          var isEmitted, outKey, outValue;
          isEmitted = true;
          outKey = key;
          return outValue = value;
        };
        ev = "mapFunc = (" + view['map'] + ').bind(this)';
        eval(ev);
        if (view['reduce'] != null) {
          eval("reduceFunc = (" + view['reduce'] + ').bind(this)');
        }
        docs = [];
        ref1 = db.data;
        for (_ in ref1) {
          if (!hasProp.call(ref1, _)) continue;
          candidate = ref1[_];
          if (!(!candidate._id.startsWith('_design/'))) {
            continue;
          }
          isEmitted = false;
          outKey = null;
          outValue = null;
          mapFunc(candidate);
          if (isEmitted) {
            docs.push({
              key: key,
              value: value
            });
          }
          if (typeof reduceFunc !== "undefined" && reduceFunc !== null) {
            reduceFunc(docs);
          }
        }
        res = docs.pluck('value');
        results.push(db.views[name].data = {
          total_rows: res.length,
          rows: res
        });
      }
      return results;
    };
    return {
      database: function(name) {
        var db;
        db = dbs[name];
        return {
          exists: function(callback) {
            return callback(null, (dbs != null ? dbs[name] : void 0) != null);
          },
          create: function(callback) {
            if (db != null) {
              return callback('db exists.');
            } else {
              db = dbs[name] = {
                views: {},
                data: {}
              };
              return callback(null);
            }
          },
          save: function(doc, callback) {
            var _, mapreduce, ref, ref1, rev, view;
            if (doc._id == null) {
              doc._id = String.generateToken(6);
            }
            rev = 0;
            if (doc._rev != null) {
              ref = doc._rev.split("-"), rev = ref[0], _ = ref[1];
            }
            rev += 1;
            doc._rev = rev + "-" + String.generateToken(6);
            console.log("===============");
            console.log(doc);
            console.log("---------------");
            console.log(db);
            console.log("===============");
            console.log("");
            db.data[doc._id] = doc;
            if (doc._id.startsWith('_design')) {
              ref1 = doc.views;
              for (name in ref1) {
                if (!hasProp.call(ref1, name)) continue;
                mapreduce = ref1[name];
                name = doc._id + '/_view/' + name;
                view = {
                  map: mapreduce['map'],
                  reduce: mapreduce['reduce'],
                  data: []
                };
                db.views[name] = view;
              }
            }
            updateViews(db);
            return callback(null, doc);
          },
          remove: function(doc, callback) {
            var _, delDoc, ref, rev;
            if ((db != null ? db.data[doc != null ? doc._id : void 0] : void 0) == null) {
              callback("doc doesn't exist.", {
                ok: false
              });
            }
            ref = doc._rev.split("-"), rev = ref[0], _ = ref[1];
            rev += 1;
            delDoc = {};
            delDoc._id = doc._id;
            delDoc._rev = rev + "-" + String.generateToken(6);
            delDoc._deleted = true;
            db.data[doc._id] = delDoc;
            return callback(null, {
              ok: true
            });
          },
          get: function(id, callback) {
            var doc, ref;
            if (id != null ? id.startsWith('_design/') : void 0) {
              return db.views[id].data;
            } else {
              doc = null;
              if (!(db != null ? (ref = db.data[id]) != null ? ref._deleted : void 0 : void 0)) {
                doc = db != null ? db.data[id] : void 0;
              }
              return callback(null, doc);
            }
          }
        };
      },
      useDb: function(name) {
        return promisify_db(this.database(name));
      }
    };
  };

}).call(this);
